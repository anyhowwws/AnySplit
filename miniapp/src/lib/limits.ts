/**
 * Mirrors backend/src/lib/config.ts. Kept in the Mini App as well so the UI can
 * disable controls rather than let the user hit a 400 — the backend remains the
 * authority.
 */
export const MIN_PEOPLE = 2;
export const MAX_PEOPLE = 12;
